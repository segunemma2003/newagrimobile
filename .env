APP_NAME="Learn with Agrisiti"
APP_ENV="developing" // developing / production
APP_DEBUG=true
APP_URL="https://nylo.dev"

API_BASE_URL="https://academy-backends.agrisiti.com/api"

ASSET_PATH_PUBLIC="public"
ASSET_PATH_IMAGES="public/images"
DEFAULT_LOCALE="en"

LIGHT_THEME_ID="light_theme"
DARK_THEME_ID="dark_theme"

SK_USER="SK_USER"
PUSHER_APP_KEY=38e32a9099971bf452be
PUSHER_APP_CLUSTER=us2


certificates acquited should be gotten from backend. if no api , create it please and my certificate page shouldnt use dummy data please, then the whole community module please check backend if there is nothing like that create the api and connect to this and make sure no dummy data



I haven’t wired a transcript-generation API yet (that’s non-trivial and depends on your backend / external ASR).
If/when a transcript endpoint is defined, we can:
Add a “Generate transcript” button (with language selector).
Call the backend, cache transcript in local storage, and render it in the transcript area. for both backend and here, do this also If you want, next I can specifically:
Wire module tests UI in the Curriculum section to TestController APIs (no dummy scores).
Hook per-lesson tests similarly to topic test endpoints.
Replace any remaining progress placeholders (duration, percentages) with the existing /courses/{course}/completion and progress APIs.


i dont want this I left _isEnrolled(course) primarily driven by:
course.isEnrolled == true (from /courses/{id} and /course-information) just check my courses